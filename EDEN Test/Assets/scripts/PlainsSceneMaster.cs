using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*

The Scene master is intended to store static variables with data about things that need to be changed everytime the scene loads, like which enemies were killed.

*/

public class PlainsSceneMaster : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
